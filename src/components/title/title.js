import './title.css';
import Logo from '../logo/logo';
import ButtonTitle from '../button-title/button-title';

const Title = () => {
    return (
        <div className='title'>
            <Logo />
            <ButtonTitle />
        </div>
    )
}

export default Title;